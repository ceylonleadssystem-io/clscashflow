{
  "name": "cls-cashflow",
  "version": "1.0.0",
  "description": "CeylonryLabs.io CashFlow — Solo/Studio/Business SaaS",
  "private": true,
  "scripts": {
    "build": "echo \"Static Supabase site: no build step required\"",
    "check": "node scripts/health-check.mjs",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.0",
    "nodemailer": "^9.0.1",
    "stripe": "^16.12.0"
  }
}
