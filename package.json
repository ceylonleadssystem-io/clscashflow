{
  "name": "cls-cashflow",
  "version": "1.0.0",
  "description": "CeylonryLabs.io CashFlow — Solo/Studio/Business SaaS",
  "private": true,
  "scripts": {
    "build": "pnpm run check && pnpm test",
    "check": "node scripts/health-check.mjs",
    "test": "node --test tests/*.test.js",
    "audit:prod": "pnpm audit --prod"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "appwrite": "17.0.0",
    "node-appwrite": "^20.0.0",
    "nodemailer": "^9.1.1",
    "stripe": "^16.12.0"
  }
}
